import React from 'react';
import ReactDOM from 'react-dom';
import App from './ex/ex2_20'

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
//sudo npx json-server --port 3001 --watch person.json
